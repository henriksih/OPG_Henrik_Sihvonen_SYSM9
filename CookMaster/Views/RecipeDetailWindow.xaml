<Window x:Class="CookMaster.Views.RecipeDetailWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mvvm="clr-namespace:CookMaster.MVVM"
        Title="RecipeDetailWindow" Height="400" Width="800">
    <Window.Resources>
        <mvvm:DateOnlyToNullableDateTimeConverter x:Key="DateOnlyConverter"/>
    </Window.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="4*"/>
        </Grid.ColumnDefinitions>
        <TextBlock Text="Receptnamn" Grid.Column="0" Grid.Row="0" Margin="5"/>
        <TextBox Text="{Binding Title, Mode=TwoWay}" Width="600" Grid.Column="1" Grid.Row="0" Margin="5" IsEnabled="{Binding IsEditing}"/>

        <TextBlock Text="Kategori" Grid.Column="0" Grid.Row="1" Margin="5"/>
        <TextBox Text="{Binding Category, Mode=TwoWay}" Width="600" Grid.Column="1" Grid.Row="1" Margin="5" IsEnabled="{Binding IsEditing}"/>

        <TextBlock Text="Ingredienser" Grid.Column="0" Grid.Row="2" Margin="5"/>
        <TextBox Text="{Binding Ingredients, Mode=TwoWay}" Width="600" Grid.Column="1" Grid.Row="2" TextWrapping="WrapWithOverflow" Margin="5" IsEnabled="{Binding IsEditing}"/>

        <TextBlock Text="Så gör man" Grid.Column="0" Grid.Row="3" Margin="5"/>
        <TextBox Text="{Binding Instructions, Mode=TwoWay}" Width="600" Grid.Column="1" Grid.Row="3" TextWrapping="WrapWithOverflow" Margin="5" IsEnabled="{Binding IsEditing}"/>

        <TextBlock Text="Datum" Grid.Column="0" Grid.Row="4" Margin="5"/>
        <DatePicker 
            SelectedDate="{Binding Date, Mode=TwoWay, Converter={StaticResource DateOnlyConverter}}" 
            Width="600" 
            Grid.Column="1" 
            Grid.Row="4" 
            Margin="5"
            IsEnabled="{Binding IsEditing}">
            <DatePicker.Resources>
                <Style TargetType="{x:Type DatePickerTextBox}">
                    <Setter Property="IsReadOnly" Value="True"/>
                    <Setter Property="Focusable" Value="False"/>
                    <Setter Property="IsHitTestVisible" Value="False"/>
                </Style>
            </DatePicker.Resources>
        </DatePicker>

        <TextBlock Text="Skapat av användare" Grid.Column="0" Grid.Row="5" Margin="5"/>
        <TextBlock Text="{Binding CurrentUser.Username}" Width="600" Grid.Column="1" Grid.Row="5" Margin="5" IsEnabled="{Binding IsEditing}"/>

        <StackPanel Grid.Column="1" Grid.Row="6" Orientation="Horizontal">
            <Button Width="180" Content="Editera receptet" Command="{Binding EditRecipeCommand}" Grid.Column="1" Grid.Row="6" Margin="20,5,5,5"/>
            <Button Width="180" Content="Spara receptet" Command="{Binding SaveRecipeCommand }" Grid.Column="1" Grid.Row="6" Margin="5" IsEnabled="{Binding IsEditing}"/>
            <Button Width="180" Content="Avbryt" Command="{Binding CancelEditCommand}" Margin="5"/>
        </StackPanel>
        
    </Grid>

</Window>
