<Window x:Class="CookMaster.Views.AddRecipeWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CookMaster.Views"
        xmlns:mvvm="clr-namespace:CookMaster.MVVM"
        mc:Ignorable="d"
        Title="Lägg till recept" Height="450" Width="800">
    <Window.Resources>
        <mvvm:DateOnlyToNullableDateTimeConverter x:Key="DateOnlyConverter" />
    </Window.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="4*"/>
        </Grid.ColumnDefinitions>
        <TextBlock Text="Receptnamn" Grid.Column="0" Grid.Row="0" Margin="5"/>
        <TextBlock Text="Kategori" Grid.Column="0" Grid.Row="1" Margin="5"/>
        <TextBlock Text="Ingredienser" Grid.Column="0" Grid.Row="2" Margin="5"/>
        <TextBlock Text="Så gör man" Grid.Column="0" Grid.Row="3" Margin="5"/>
        <TextBlock Text="Datum" Grid.Column="0" Grid.Row="4" Margin="5"/>
        <TextBlock Text="Skapat av användare" Grid.Column="0" Grid.Row="5" Margin="5"/>
        <TextBox Text="{Binding Title, Mode=TwoWay}" Width="600" Grid.Column="1" Grid.Row="0" Margin="5" />
        <TextBox Text="{Binding Category, Mode=TwoWay}" Width="600" Grid.Column="1" Grid.Row="1" Margin="5"/>
        <TextBox Text="{Binding Ingredients, Mode=TwoWay}" Width="600" Grid.Column="1" Grid.Row="2" Margin="5"/>
        <TextBox Text="{Binding Instructions, Mode=TwoWay}" Width="600" Grid.Column="1" Grid.Row="3" Margin="5"/>
        <DatePicker 
            SelectedDate="{Binding Date, Mode=TwoWay, Converter={StaticResource DateOnlyConverter}, UpdateSourceTrigger=PropertyChanged}" 
            Width="600" 
            Grid.Column="1" 
            Grid.Row="4" 
            Margin="5">
            <DatePicker.Resources>
                <Style TargetType="{x:Type DatePickerTextBox}">
                    <Setter Property="IsReadOnly" Value="True"/>
                    <Setter Property="Focusable" Value="False"/>
                    <Setter Property="IsHitTestVisible" Value="False"/>
                </Style>
            </DatePicker.Resources>
            </DatePicker>
        <TextBlock Text="{Binding CurrentUser.Username}" Width="600" Grid.Column="1" Grid.Row="5" Margin="5"/>
        <Button Width="200" Content="Lägg till receptet" Command="{Binding AddRecipeCommand}" Grid.Column="1" Grid.Row="6" Margin="5"/>
    </Grid>
</Window>